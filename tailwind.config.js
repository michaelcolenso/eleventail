module.exports = {
    theme: {
        boxShadow: {
            inner: 'inset 0 -9px 0 #FCBF49'
        }

    },
    variants: {},
    plugins: [
        require('@tailwindcss/custom-forms'),
    ]
}